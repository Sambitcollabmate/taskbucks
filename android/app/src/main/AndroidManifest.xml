<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Every real API call the app makes (auth, tasks, wallet, ads) needs
         this. The debug/profile manifests already declared it for the
         Flutter tool's own use, but there was no release-build source of
         it; a published release APK would have had no network access
         at all. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Android 13+ (API 33) requires runtime consent before any notification
         can be shown. Requested at a deliberate, explained moment (right
         after a user's first task completion, see NotificationPermissionSheet)
         rather than an unexplained system dialog on launch. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <application
        android:label="EarnBucks"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- The App ID itself doesn't serve specific ads, so it's safe to
             use the real one even during development; it's the ad UNIT
             ID (in ad_mob_service.dart) that must stay a Google test ID
             until earnbucks-api has a public URL for real SSV. -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-6514822944587484~8441061947" />

        <!-- image_cropper's underlying UCrop activity is not auto-merged by
             the plugin; must be declared explicitly or cropImage() crashes
             with ActivityNotFoundException. -->
        <activity
            android:name="com.yalantis.ucrop.UCropActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Required for url_launcher to find an email app for the
             mailto: links on the Contact screen (Section 3.11+ package
             visibility). -->
        <intent>
            <action android:name="android.intent.action.SENDTO"/>
            <data android:scheme="mailto"/>
        </intent>
    </queries>
</manifest>
